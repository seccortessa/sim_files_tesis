%frio =255;
calor=1000;
%cachete izquierdo
for c= 430:550
    for m=550:700
        %bluefrio(c,m)=frio;
        redcalor(c,m)=calor;        
    end
end

%cachete derecho
for c= 470:600
    for m=300:400
        %bluefrio(c,m)=frio;
        redcalor(c,m)=calor;
    end
end

%labios

for c= 550:600
    for m=440:530
        %bluefrio(c,m)=frio;
        redcalor(c,m)=380;
    end
end